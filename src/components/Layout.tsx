/**
 * @deprecated DEAD CODE — Layout.tsx is orphaned. The active UI lives in App.tsx
 * under PomodoroProvider. This file and its dependents (TimerControls, SettingsPanel,
 * MobileTabBar, Onboarding, useKeyboard, useTheme) are stubbed to prevent broken
 * imports. Safe to delete once the team confirms no references remain.
 */
export default function Layout() {
  return null;
}
