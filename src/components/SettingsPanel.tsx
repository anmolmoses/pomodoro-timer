/**
 * @deprecated DEAD CODE — Orphaned component that depended on contexts/SettingsContext.
 * The active equivalent is src/components/SettingsModal.tsx (uses PomodoroProvider).
 * Stubbed to a no-op to prevent broken imports. Safe to delete.
 */
export default function SettingsPanel(_props: { open: boolean; onClose: () => void }) {
  return null;
}
