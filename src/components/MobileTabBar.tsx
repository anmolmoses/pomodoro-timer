/**
 * @deprecated DEAD CODE — Orphaned component only used by Layout.tsx.
 * Stubbed to a no-op to prevent broken imports. Safe to delete.
 */
export type Tab = 'timer' | 'stats' | 'settings';

export default function MobileTabBar(_props: { active: Tab; onChange: (t: Tab) => void }) {
  return null;
}
