/**
 * @deprecated DEAD CODE — Depended on contexts/TimerContext (orphaned).
 * Keyboard shortcuts for the active UI should be added to a new hook
 * that consumes PomodoroProvider if needed.
 * Stubbed to a no-op to prevent broken imports. Safe to delete.
 */
export function useKeyboard(_onToast?: (msg: string) => void): void {
  // no-op
}
