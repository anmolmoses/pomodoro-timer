/**
 * @deprecated DEAD CODE — Only used by Layout.tsx (orphaned).
 * The active UI uses a fixed dark theme via Background.tsx.
 * Stubbed to a no-op to prevent broken imports. Safe to delete.
 */
export function useTheme() {
  return { dark: true, toggle: () => {} };
}
